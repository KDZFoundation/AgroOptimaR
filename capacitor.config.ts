import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
    appId: 'pl.agrooptima.app',
    appName: 'AgroOptima',
    webDir: 'out',
    server: {
        androidScheme: 'https',
    },
}

export default config
